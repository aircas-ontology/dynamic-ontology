# AGENT.md

## 1. 规则等级

本规范仅使用两类规则：

- **【必须】**：硬性要求，必须执行。
- **【禁止】**：硬性要求，禁止执行。
- **【优先】**：默认执行；确需偏离时在 Plan 中说明原因。

规则优先级：

```text
用户明确要求 > AGENT.md > 仓库现有约定 > 通用最佳实践
```

发生冲突时：

- 【必须】说明冲突及影响，等待用户确认后再执行。

---

## 2. 执行流程

涉及代码、组件、样式、配置、依赖、Mock、文件增删改时：

1. 【必须】先只读检查与当前任务直接相关的目录、文件和现有实现。
2. 【必须】根据实际检查结果编写 Plan，并等待用户确认。
3. 用户提出调整意见时，【必须】更新 Plan 并重新提交确认。
4. 用户确认后，【必须】将最终 Plan 保存到 `plans/YYYYMMDD/`，再开始实施。
5. 【必须】实施内容不得超出最终确认的修改范围。
6. 实施中如发现必须扩大范围、改变核心技术路线或新增依赖，【必须】停止相关实施，更新 Plan 并重新确认。

以下任务不要求执行完整 Plan 流程：

- 只读分析；
- 问题定位；
- 代码解释；
- 修改建议；

Plan 至少包含：

- 命名：参考[1-\*.md\\2-*.md]
- 需求理解；
- 修改范围；
- 新增 / 修改 / 删除文件；
- 核心实现方式；
- 新增依赖及必要性；
- 验证方式。

---

## 3. 基本原则

- 【必须】只修改当前任务明确涉及的内容。
- 【优先】采用满足需求的最小修改方案。
- 【优先】沿用仓库现有目录、组件、类型、样式、工具和实现方式。
- 【优先】复用已有依赖、组件和能力，不为未来需求提前设计复杂抽象。
- 【禁止】顺带重构、批量格式化或调整与当前任务无关的内容，。
- 【必须】保持类型安全，不通过规避类型检查解决问题。
- 【必须】修改结果必须能够通过实际运行、检查或构建进行验证。
- 【必须】不确定或需要扩大修改范围时，必须写入 Plan 并等待确认。
- 【禁止】修改或提交 `html/` 生产构建目录。
- 【必须】使用`Subagent` 前获得用户确认。
- 【必须】增量输出：只输出修改过的代码部分。

> **核心原则：先检查、先 Plan、确认后实施、最小修改、复用现有能力、类型安全、真实验证。**

---

## 4. 项目结构与规范

**目录职责**: 本项目存放源代码，包含以下目录：

```text
src/
├─ api/             HTTP 接口定义，按页面 / 业务域分类
├─ assets/          构建期静态资源，按页面 / 业务域分类
├─ components/      公共组件，由 register 统一注册
├─ example/         参考资料目录，不作为正式业务依赖
├─ layout/          公共布局
├─ mock/            Mock / 样例数据，按页面 / 业务域分类，符合正式业务类型定义
├─ models/          业务模型或 Class
├─ router/          Vue Router
├─ stores/          Pinia Store，Store 仅维护需要共享的业务状态，禁止直接操作 DOM
├─ styles/          全局主题及公共样式
├─ types/           TypeScript 类型，按页面 / 业务域分类，定义可复用业务类型
├─ utils/           无业务状态的公共工具函数
├─ views/           业务页面
├─ App.vue          根组件
└─ main.ts          入口文件

public/             禁止修改
├─ configs/         部署后可调整的运行时配置
└─ data/            部署后可调整的运行时数据

docs/               Plan 和项目文档
html/               生产构建产物，禁止读取
```

- 【必须】`src/` 业务源码使用 TypeScript。
- 【必须】Vue 使用 Vue 3、Composition API、`<script setup lang="ts">`。
- 【必须】api、assets、mock、types等目录，内部需要按照页面 / 业务域进行分类管理。
- 【优先】项目内部模块引用使用 `@/` 路径别名。

---

## 5. 文件与命名

- 【必须】Vue 页面目录和组件文件使用 PascalCase，`index.vue`、`App.vue` 等生态约定除外。
- 【必须】普通 TypeScript 文件使用 camelCase；以 Class 为主要导出的模型文件使用 PascalCase。
- 【必须】Store 文件使用 `use<Domain>Store.ts`。
- 【必须】样式、图片、图标及资源目录使用 kebab-case。
- 【必须】文件名与 import 路径大小写保持一致。

---

## 6. UI / SCSS

- 【必须】项目样式统一使用 SCSS；组件私有样式使用 `scoped lang="scss"` 就近维护。
- 【必须】样式修改复用现有 Design Token、CSS Variables、Class、布局体系和页面设计语言，不得另建独立样式体系。
- 【必须】业务视觉颜色优先使用现有主题变量；新增颜色变量统一使用 `--aircas-color-*`，禁止直接硬编码 HEX / RGB / HSL / `white` / `black`。
- 【必须】CSS Class 使用明确业务语义的 kebab-case；BEM 使用完整类名，禁止通过 `&__xxx`、`&--xxx`、`&-xxx` 拼接。
- 【必须】SCSS 嵌套原则上不超过 4 层，优先通过稳定 Class 定位。
- 【必须】正式功能图标复用项目现有图标体系，禁止使用 Emoji、Unicode 字符代替；纯图标按钮必须提供 Tooltip、`title` 或 `aria-label`。
- 【必须】第三方组件样式优先通过官方 API、Props、CSS Variables、Theme 或稳定 Class 修改，避免依赖内部 DOM。
- 【必须】检查正常态、hover、active、disabled、overflow 等状态；
- 【优先】常规布局使用 Flex / Grid；
- 【优先】Element Plus 已有适用组件时直接复用，部分样式已重新定义[src/styles/element-plus]，使用方法[class="aircas-*"]。

视觉一致性：

- 【优先】以 `1920×1080` 为基准设计，并兼容更高桌面分辨率。
- 【优先】整体保持暗色科技风、紧凑信息密度，并与现有系统视觉风格一致。
- 【优先】统一视觉层级：
  - 页面标题： `20px`
  - 区域标题： `16px`
  - 正文： `14px`
  - 辅助信息： `12px`
  - 常规表单控件 / 按钮高度：`32px`
  - 常用间距： `8 / 12 / 16 / 20px`
- 【禁止】无明确原因使用零散字号、间距、控件尺寸或独立视觉规则。
- 【优先】交互组件检查 normal、hover、active、disabled、focus、overflow 等实际适用状态。

---

## 7. 异步数据

涉及异步数据的页面或组件：

- 【必须】处理 loading、empty、error 状态。
- 【必须】请求失败时提供明确错误处理，不静默吞掉异常。
- 【必须】避免请求进行中重复无控制触发相同请求。
- 【优先】页面卸载后取消不再需要的请求。

---

## 8. 依赖与第三方能力

- 【优先】使用仓库当前已经安装的依赖、组件和工具。
- 【必须】新增依赖前确认现有能力无法合理完成需求，并在 Plan 中说明必要性。
- 【必须】依赖发生变化时保持 `package.json` 与 lockfile 一致。

对于 ECharts、Three.js、Cesium、Mars3D、OpenLayers 等第三方实例：

- 【优先】集成cesium-mcp-dev服务，使用cesum-skills相关skills
- 【必须】组件销毁时释放实例、监听器、定时器、Observer、RAF 及相关资源。
- 【必须】避免重复初始化导致实例、Canvas、Overlay 或 GPU 资源残留。
- 【禁止】使用 `reactive()` 深度代理 Cesium Viewer、Map、Chart、Layer、Graphic 等第三方复杂实例。

---

## 9. 验证

修改完成后：

- 【必须】执行项目实际存在的 TypeScript 类型检查，以 package.json -> scripts 为准。
- 【必须】执行 Production Build。
- 【必须】验证结果必须与实际执行情况一致；失败时明确说明失败项、原因和未解决问题。

---

## 10. 交付

修改完成后，简要说明：

```text
修改文件：
实现内容：
新增依赖：有 / 无
验证结果：
未完成事项：
```

- 【必须】只记录真实发生的修改和真实执行的验证结果。
- 【必须】如与最终 Plan 存在差异，应明确说明。
- 【优先】交付说明保持简洁，不重复粘贴未修改代码或大段 Plan 内容。
