# 页面框架布局与登录跳转开发 Prompt

## 需求背景
参考原型项目 `E:\资料\h数字5模块\project\github-project`，在当前项目实现应用页面框架布局，并让登录页按钮具备跳转能力。

## 开发目标
1. 实现顶部标题栏。
2. 实现左侧菜单栏。
3. 实现页面导航信息面包屑区域。
4. 将页面框架布局接入当前项目。
5. 修改登录页登录按钮：用户名和密码校验通过后，跳转到页面框架布局。

## 原型参考文件
- 顶部标题栏：`E:\资料\h数字5模块\project\github-project\src\layout\index.vue`。
- 侧边菜单栏：`E:\资料\h数字5模块\project\github-project\src\layout\components\NavigationMenu.vue`。
- 页面导航面包屑：`E:\资料\h数字5模块\project\github-project\src\layout\components\BreadcrumbBar.vue`。
- 登录页：当前项目 `src/views/LoginPage/index.vue`。

## 实现要求
- 遵守根目录 `AGENTS.md` 以及 `src/layout`、`src/views`、`src/router`、`src/styles` 的目录规范。
- 使用 Vue 3 Composition API 和 `<script setup lang="ts">`。
- 布局负责公共结构、区域编排和布局级交互，不承载页面业务。
- 使用现有 Aircas 主题变量，兼容暗色和亮色主题。
- 通过路由出口承载页面内容。
- 登录页保留用户名和密码非空校验，校验成功后执行路由跳转。
- 不修改 `html/` 和 `public/`。
- 不新增无必要依赖。

## 建议修改范围
- `src/App.vue`
- `src/layout/index.vue`
- `src/layout/components/`
- `src/views/LoginPage/index.vue`
- `src/router/index.ts`

## 验证要求
- `npm test`
- `npm run test:coverage`
- `npm run type-check`
- `npm run build:verify`
- `npm run check:project-conventions`
- `git diff --check`
