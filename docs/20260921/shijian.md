## 1 本体空间列表字段变更和新增

## 需求背景

D:\projectcode\sthq\dynamic-ontology\docs\20260916\ontologyListApi-guoshengnan.md 该文档有修改，增加了创建时间和更新时间，根据文档增加或修改原有的创建时间和更新时间字段对接，同时更新mock数据

## 2 创建本体空间功能增加图标

## 需求背景

之前调用创建空间接口时没传'icon'参数，现在在调用创建空间接口前，如果用户上传了空间图标，就把空间的图标url放在'icon'参数中。

## 具体步骤

用户上传完图片后就根据"上传图片文件获取缩略图urlApi-shijian.md"文档调用接口，展示出图标缩略图，记录url地址，待调用创建空间接口时，把url放到'icon'参数中传递。

## 注意事项

编辑空间的缩略图也是同样的逻辑
