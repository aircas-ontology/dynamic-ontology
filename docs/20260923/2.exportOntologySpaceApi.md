# 接口名称

- 导出本体空间（含分类树、全部本体 schema 与实例数据）

## 编译位置

- apis：`src/apis/ontologyManageApi.ts`
- types：`src/types/apis/exportOntologySpaceType.ts`
- mocks：`src/mocks/exportOntologySpaceMock/`

成功响应结构未在文档中声明。响应 data 类型和 Mock 成功样例待文档补充后再生成。

## 接口描述

- 导出本体空间，包含分类树、全部本体 schema 与实例数据。
- 在线文档：本体空间管理 / `OntologySpaceController_exportOntologySpace`。
- OpenAPI 路径为 `/space/export`，服务前缀为 `/ontology`。项目请求路径使用加前缀后的完整路径。

## 接口 domain

- `DOMAIN_CONFIG.ONTOLOGYMANAGE_URL`

## 接口 uri

- `/ontology/space/export`

## 请求方式

- GET

## 输入参数

- Query 参数，通过 URL 查询串传递。在线文档未给出参数说明和默认值。

| 参数名  | 类型            | 必填 | 说明       |
| ------- | --------------- | ---- | ---------- |
| spaceId | integer / int32 | 是   | 文档未说明 |

请求示例：

```text
GET /ontology/space/export?spaceId=1
```

## 输出参数

在线文档只声明 HTTP `200`，描述为 `OK`。该操作没有 `content`、`schema` 或响应模型，因此不能确定成功结果是 JSON、文件流或其他形式。下面不编造响应字段。

## code

- 该操作在文档中声明的响应为 `200`。
- 文档没有列出 `code` 枚举。

## message

- 文档没有声明 `message`。

## types 示例

文件：`src/types/apis/exportOntologySpaceType.ts`

只根据已声明的查询参数编写。响应 data 类型待成功响应结构确认后再补。

```ts
/** 导出本体空间的查询参数。 */
export interface ExportOntologySpaceParams {
  /** 本体空间 id，必填。文档未提供字段说明。 */
  spaceId: number;
}
```

并在 `src/types/index.ts` 中按字典序统一导出：

```ts
export type { ExportOntologySpaceParams } from "./apis/exportOntologySpaceType";
```

## apis 示例

文件：`src/apis/ontologyManageApi.ts`

返回类型依赖未声明的成功响应，这里不写成 `ApiResponse`。请求方式、domain 和 URI 以正文为准。

```ts
import type { ExportOntologySpaceParams } from "@/types";

/**
 * @description 导出本体空间，包含分类树、全部本体 schema 与实例数据。
 *
 * 请求方式：GET `/ontology/space/export`
 *
 * @param params 查询参数。
 * @param params.spaceId 本体空间 id，必填。
 * @returns 文档未声明成功响应结构，返回类型待确认后再实现。
 */
export function getExportOntologySpaceInterface(params: ExportOntologySpaceParams): Promise<unknown> {
  return request({
    url: DOMAIN_CONFIG.ONTOLOGYMANAGE_URL + "/ontology/space/export",
    method: "get",
    params,
  });
}
```

并在 `src/apis/index.ts` 中导出：

```ts
export { getExportOntologySpaceInterface };
```

## mocks 示例

文档没有成功响应样例，不生成 Mock。
