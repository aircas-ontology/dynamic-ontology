# 接口名称

- 导出单个本体（含 schema 与实例数据）

## 编译位置

- apis：`src/apis/ontologyObjectManageApi.ts`
- types：`src/types/apis/exportOntologyType.ts`
- mocks：`src/mocks/exportOntologyMock/`

成功响应结构未在文档中声明。响应 data 类型和 Mock 成功样例待文档补充后再生成。

## 接口描述

- 导出单个本体，包含 schema 与实例数据。
- 在线文档：本体对象管理 / `OntologyMetaController_exportOntology`。
- OpenAPI 路径为 `/meta/export`，服务前缀为 `/ontology`。项目请求路径使用加前缀后的完整路径。

## 接口 domain

- `DOMAIN_CONFIG.ONTOLOGYMANAGE_URL`

## 接口 uri

- `/ontology/meta/export`

## 请求方式

- GET

## 输入参数

- Query 参数，通过 URL 查询串传递。在线文档未给出参数说明和默认值。

| 参数名           | 类型   | 必填 | 说明       |
| ---------------- | ------ | ---- | ---------- |
| uniqueIdentifier | string | 是   | 文档未说明 |

请求示例：

```text
GET /ontology/meta/export?uniqueIdentifier=d3e1b0c27f29452eb7cadd1f51eac535
```

## 输出参数

在线文档只声明 HTTP `200`，描述为 `OK`。该操作没有 `content`、`schema` 或响应模型，因此不能确定成功结果是 JSON、文件流或其他形式。下面不编造响应字段。

## code

- 该操作在文档中声明的响应为 `200`。
- 文档没有列出 `code` 枚举。

## message

- 文档没有声明 `message`。

## types 示例

文件：`src/types/apis/exportOntologyType.ts`

只根据已声明的查询参数编写。响应 data 类型待成功响应结构确认后再补。

```ts
/** 导出单个本体的查询参数。 */
export interface ExportOntologyParams {
  /** 本体唯一标识，必填。文档未提供字段说明。 */
  uniqueIdentifier: string;
}
```

并在 `src/types/index.ts` 中按字典序统一导出：

```ts
export type { ExportOntologyParams } from "./apis/exportOntologyType";
```

## apis 示例

文件：`src/apis/ontologyObjectManageApi.ts`

返回类型依赖未声明的成功响应，这里不写成 `ApiResponse`。请求方式、domain 和 URI 以正文为准。

```ts
import type { ExportOntologyParams } from "@/types";

/**
 * @description 导出单个本体，包含 schema 与实例数据。
 *
 * 请求方式：GET `/ontology/meta/export`
 *
 * @param params 查询参数。
 * @param params.uniqueIdentifier 本体唯一标识，必填。
 * @returns 文档未声明成功响应结构，返回类型待确认后再实现。
 */
export function getExportOntologyInterface(params: ExportOntologyParams): Promise<unknown> {
  return request({
    url: DOMAIN_CONFIG.ONTOLOGYMANAGE_URL + "/ontology/meta/export",
    method: "get",
    params,
  });
}
```

并在 `src/apis/index.ts` 中导出：

```ts
export { getExportOntologyInterface };
```

## mocks 示例

文档没有成功响应样例，不生成 Mock。
