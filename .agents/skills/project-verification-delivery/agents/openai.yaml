interface:
  display_name: "项目验证与交付"
  short_description: "根据项目实际脚本验证修改，并生成可追溯且可信的交付结果摘要"
  default_prompt: "Use $project-verification-delivery to verify these changes and prepare the final handoff."
policy:
  allow_implicit_invocation: true
