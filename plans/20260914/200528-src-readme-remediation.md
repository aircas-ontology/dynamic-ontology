# 按 `src/readme.md` 规范完成现状整改

## 需求理解

处理 12 个 `src/*/readme.md` 中“现状差异与后续整改”列出的实际问题；采用安全占位登录，删除无业务实现的占位代码，不实施可选的大文件拆分。源码整改完成后删除全部整改章节。

## 修改范围与实现方式

- 清理空本体页面、路由、Mock、空布局和无效守卫，路由仅保留登录及 404 回退。
- 登录页移除演示凭据、密码持久化、假 token 和无效跳转，保留校验及未接入提示。
- 按页面/组件归属迁移并重命名资源，删除重复、无引用资源和 `src` 内 `.DS_Store`。
- 修正公共组件主题色、按钮语义、键盘焦点、图标替代文本及时间轴生命周期。
- 收紧 `SatelliteClass` 状态与输入边界；为 `TimeEngine`、Mars3D Viewer 增加显式所有权和幂等清理。
- 统一 Request/Storage 错误契约，补齐业务类型字段注释。
- 不新增依赖，不修改 `public/`、`html/` 或无关文件。

## 文件变化

- 新增模型、工具和静态规范测试。
- 修改路由、登录页、公共组件、模型、工具、类型和 12 个目录规范。
- 移动登录页与面板资源；删除空占位及无引用资源。

## 验证

依次执行 `npm test`、`npm run test:coverage`、`npm run check:types-conventions`、`npm run type-check`，并将 Vite 构建输出到临时目录；最后执行本地浏览器登录、键盘、主题、资源和控制台烟测。
